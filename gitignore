# Dataset (too large for GitHub — view README for download instructions)
# The Dataset/ folder itself is kept via Dataset/.gitkeep; only its contents are ignored.
Dataset/*
!Dataset/.gitkeep

# Python
__pycache__/
*.py[cod]
*.egg-info/
venv/
.venv/

# Jupyter
.ipynb_checkpoints/

# OS / editor
.DS_Store
.vscode/
.idea/
